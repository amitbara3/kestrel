---
name: Bug report
about: Something behaves incorrectly
labels: bug
---

**What happened**

**What you expected**

**Reproduction**

```bash
# smallest sequence of requests that shows it
```

**Environment**
- Kestrel version / commit:
- Drivers: `memory` or `redis`/`postgres` (check the boot log)
- Node version:
- Replica count:

**Request ID**
<!-- The `requestId` from the error envelope or the `X-Request-Id` header. -->

**Relevant log lines**
